Package `x` provides `x packages` that meant to be feature extensions to standard or other 3rd party packages.
For example: `xhttp` pkg has a func called `ParseJSON()` where standard `net/http` pkg does not have.
